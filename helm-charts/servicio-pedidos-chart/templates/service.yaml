apiVersion: v1
kind: Service
metadata:
  name: {{ include "servicio-pedidos-chart.fullname" . }}
  labels:
    {{- include "servicio-pedidos-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "servicio-pedidos-chart.selectorLabels" . | nindent 4 }}
