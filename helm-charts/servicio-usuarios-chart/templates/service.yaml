apiVersion: v1
kind: Service
metadata:
  name: {{ include "servicio-usuarios-chart.fullname" . }}
  labels:
    {{- include "servicio-usuarios-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "servicio-usuarios-chart.selectorLabels" . | nindent 4 }}
